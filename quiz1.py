# Graded by Whitney Barber
# Barwhi18

from lab2 import n
# do not use single (or any) quotes around imported variables
# do not indicated file suffix (i.e. .py, .txt. etc.)

for i in range(n):
  if i>0:
      print "Hello World"
# indent 'print' two spaces (or however many - just keep it consistent)